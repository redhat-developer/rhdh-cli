{{!--
  RHDH overlay for the upstream @backstage/cli-module-new 0.1.6
  catalog-processor-module README.md.hbs. The upstream file is minimal but
  refers to "the Backstage CLI" as the authoring tool. This overlay replaces
  it with RHDH-focused language.

  When the RHDH release targeting @backstage/cli-module-new 0.1.7+
  (Backstage 1.55.0) is supported, review whether upstream has improved the
  standalone-project README and remove this overlay if it has caught up.
--}}
# {{packageName}}

The {{fullModuleId}} module for the RHDH Software Catalog.

