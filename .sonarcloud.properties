# Parallel E2E specs share the same flow; only repo URL and paths differ.
# Exclude from duplication (CPD) only — they remain analyzed for issues and coverage.
sonar.cpd.exclusions=e2e-tests/community-plugins-build-package.test.ts,e2e-tests/rhdh-plugins-build-package.test.ts
